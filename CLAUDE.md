<tools>
- To maximize efficiency, try to use tools in parallel as much as possible `Task`. Delegate to subagents where possible to preserve context window
- Proactively use the `AskUserQuestion` tool for clarification
</tools>
